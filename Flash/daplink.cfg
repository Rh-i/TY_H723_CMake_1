# 选择CMSIS-DAP接口
source [find interface/cmsis-dap.cfg]

# Horco v0.2 的 USB bulk 端点在当前环境会对 CMD_INFO 无响应，固定使用已验证的 HID 后端
cmsis-dap backend hid

# 选择传输协议
transport select swd

# 选择STM32H7目标
source [find target/stm32h7x.cfg]
