# 任务清单
你现在的任务是基于前面我们所完成的代码框架，完成其对应的实现

[x] 完成`Online`类的实现
[x] 在`./User/App/task/`中实现`sys_task`内容，使其执行频率为 1000 Hz，并将 `Online::update()` 放入其中实现 Online 对象的实时更新。（后续该任务可能会被分到更多的小功能来监看整个工程的运行）
[x] 完成`DjiMotor`和`DjiMotorBus`的实现
[x] 在`./User/App/task/`中新建文件`dji_motor_task.cpp`，把维护dji_motor的任务放在其中。
[x] 测试能否调用接口来驱动CAN1上电机id为2的M3508电机

# 要求
1. 完成具体实现时，已有声明的函数必须按照注释来完成功能实现
2. 你可以在必要时在类中添加private成员函数，或者在对应cpp中添加static辅助函数
3. 每个任务在完成后必须进行测试校验
4. 所有测试代码按测试内容分类储存在`./User/App/test/`中，例如要测试电机转动就在`./User/App/test/motor/`中创建文件，要测试`Online`对象就在`./User/App/test/online_check/`中创建文件。通过是否全局挂起这些测试任务（或者是否编译这些测试任务）来有针对性的测试相关内容
5. 不得修改`./Core/`中的代码。如果没办法绕开就和用户说。不修改无关内容。不删除已有定义。
6. 对电机有疑问的，优先查看`./docs/`中的文件

## 验证记录（2026-09-01）

- `Online` 自检通过：初始离线、任务刷新上线、超时前在线、超过阈值离线。
- `sys_task` 与 `dji_motor_task` 实机连续运行 15590 个周期，循环计数一致，验证为 1 kHz。
- CAN1 ID2 M3508 测试通过：20330 帧反馈全部成功分发，峰值转速 3532 rpm。
- FDCAN1 发送/接收错误计数和 Bus-Off 均为 0，测试结束后四个控制槽位均为零。
- 测试开关已恢复为关闭，最终烧写固件不会在复位后主动驱动电机。
